# A3 脱敏验证记录

日期：2026-09-22。结论：**A3 限定的受控资源/包探针通过**，零模型调用。资源 9/9，实际 npm/Git 包管理 8/8；A0–A2 回归均通过。完整技能库、市场激活事务、产品 Run 和 M0-SDK 尚未完成。

基线：`73802d4d6526aeb6be59d91278e0d5223a7eea9d`。分支：`codex/a3-resource-probe`。**实际被测代码提交：`c2378dd0a9c2f54daa50bc4cf843e044d3c484b2`**。先提交代码，再在干净工作树及该 SHA 的独立 git archive 副本实测，最后新增本报告与 SSOT 文档；本报告不冒充自身文档提交已被提前测试。

## 环境和输入

macOS 27.0，机器与 Node 进程均 arm64，Rosetta=0。沿用 Git 2.55.0（`/opt/homebrew/bin/git`）、Python 3.14.7（`<repo>/.venv/bin/python`；系统 python3 位于 `/opt/homebrew/bin/python3`），项目 Node 24.21.0 / npm 11.19.0（`<repo>/.artifacts/toolchains/node-v24.21.0-darwin-arm64/bin/`）。没有打印环境变量、token 或密钥值。

Pi 仍为 0.87.0，TypeScript 7.0.2，MCP 类型 peer 和 A0 声明补丁不变。唯一应用锁文件 SHA-256 仍为 `7cd35e4fc73c7e0b61112b515c6389505baf7d541e638bd7cf0ffe2dbf6e8e44`。未增加应用依赖、包管理器或 Pi 运行时补丁。

重新核对缓存官方 Pi tarball 的 SRI，16 个安装文件与 tarball 逐字节一致，包括根 exports/声明、资源/包/技能/Session 实现及官方 SDK/包文档和 skills 示例。使用 Context7 查询官方资料，再以锁定发行包确定行为；不将 main 的资料当作发行版本保证。详情见 [来源、SRI 与文件摘要](a3-inputs.json)。未验证发行签名。

为实际安装探针单独获取 `is-number@6.0.0`、`is-number@7.0.0`、`is-odd@3.0.1`：registry 精确版本元数据、SRI、字节数、SHA-256 均核对，npm 自己准备缓存。它们是**仅安装、从不 import/执行**的测试输入，记录在 [package-fixtures.json](../../packages/pi-adapter/package-fixtures.json)，不是新增应用依赖或第二个应用锁文件。临时安装树的 npm lock 逐项检查版本/SRI。

独立新副本通过官方 Node 下载重新取得相同 52,909,993 字节归档，SHA-256 为 `bed7eea5325e1108f32ce5228ddd6a5f0f08a499ee42aa7442aea583702f6057`，与官方 SHASUMS256 一致；仍未做签名验证。

## 复用和新增

复用 Pi 公开 `loadSkills`、`loadSkillsFromDir`、`formatSkillsForPrompt`、`ResourceLoader`、`DefaultResourceLoader`、`DefaultPackageManager`、内存 SettingsManager、原生 Session 的 reload/waitForIdle、原生历史和现有空凭据 services。resolve/install/update/listConfiguredPackages 是类方法，Skill/ResourceLoader/ProgressEvent 等是类型，不作为根级运行时导出。没有 experimental 或内部源码 import。

只新增完整内容目录的批准摘要/只读副本、显式资源加载与准入检查、两份探针测试、测试输入清单与下载准备入口；扩展已有隔离启动器为 `--resources` / `--packages`。没有技能 parser、npm/Git parser、安装器、Harness、RunCoordinator、Provider 或完整市场。tsconfig 增加 erasableSyntaxOnly，让编译检查覆盖 Node 原生 TypeScript strip-only 限制。

## 实际覆盖

| 范围 | 已运行断言 |
|---|---|
| 预加载批准 | 全局/项目/包内合成扩展不执行；只将预先批准 inline factory 交给 Pi；AGENTS、APPEND_SYSTEM、默认技能均不混入 |
| 内容快照 | Pi 原格式解析/诊断/提示格式；整个包内模板与脚本字节一并复制；源 A→B 不改 A；拒绝链接和声明的外部依赖；普通写入失败，owner 篡改被后续 hash 核验发现 |
| 相邻资源准入 | Pi Session 实际 reload，确认 loaded 锁和 system prompt 后再提交合成锁记录；host 未 settled、加载错误/技能缺失、保存失败、重入或选择变化均阻断 |
| 禁用 | 后续技能清单/system prompt 不再包含技能；Pi 原生历史不改写；显式拒绝的 read 工具仍拒绝，不因技能加载扩大授权 |
| npm | 两个全新根显式安装固定 6.0.0/7.0.0，旧目录保持；同根版本共享路径；update 跳过固定 npm；真实嵌套共享依赖与 lock SRI |
| 来源和失败 | resolve skip/error；Pi offline 快捷返回；已配置未安装、ENOTCACHED/目标版本缺失失败不持久化来源；local 来源是可变引用，设置变化不是激活或删除 |
| Git | 合成本地仓库两个真实提交/tag；Pi clone/checkout/依赖安装；固定 ref 新根安装和旧测试根原地 reconcile；错误 ref 清理；旧独立根保留 |
| 生命周期 | 合成 preinstall/install/postinstall/prepare canary 均不执行，即使包内 .npmrc 指定 ignore-scripts=false；CLI 始终显式禁用 scripts |
| 公开缺口 | 实际证明 DefaultResourceLoader 的 no* 标志不阻止缺包安装；无仓库边界的 resolve 尝试祖先发现而被拒绝 |

资源 9/9、包管理 8/8，无 skip/todo。A/B 是明确的合成无模型准入场景，Git 包及其 canary 也是合成输入；没有伪造模型事件或真实模型 fixture。没有调用 prompt、真实 Provider、系统密钥库或用户 auth.json。

## 命令和结果

命令使用上述精确 Node/npm 和已有 Python；文档类型检查的 PATH 另含 `<repo>/node_modules/.bin`。下表是在真实代码提交上执行的结果，非计划。下载准备的连续两次记录来自初始代码提交 `968414c7eca36c9d0b563fe03542d8e9a0c2dc6f`，其准备入口/输入清单在最终提交完全相同；最终提交的新副本另完成一次独立准备。其余回归及下述新副本两轮均针对上列最终被测提交。

| 命令 | 结果 |
|---|---|
| `.venv/bin/python scripts/check-ssot.py` | 开工基线及提交回归均通过，60 项 |
| `.venv/bin/python scripts/test-tools.py` | 基线及回归均通过，15 项 |
| `.venv/bin/python scripts/check-docs.py --structural-only` | 基线及回归通过；startup 27 个摘要不变 |
| `.venv/bin/python scripts/check-docs.py --typecheck` | 16 个历史示例及协议类型检查通过 |
| `.venv/bin/python scripts/bootstrap.py --offline` | 默认文档行为连续两次通过 |
| `.venv/bin/python scripts/bootstrap.py --app --offline` | 应用初始化连续两次通过，锁不变，脚本禁用 |
| `npm run prepare:pi-packages` | 下载阶段连续两次通过，3 个固定输入 byte/SRI 审计通过；不导入 SDK |
| `npm run check:environment` | 版本、架构和工具位置核对通过 |
| `npm run typecheck` / `npm run test:pi-types` | 严格完整声明检查通过；类型/补丁 10/10 |
| `npm run test:pi-probe` | A1 Session 10/10 |
| `npm run test:pi-tools` / `npm run test:pi-shell` | A2 工具 19/19、真实 macOS Bash 4/4 |
| `npm run test:pi-resources` / `npm run test:pi-packages` | A3 9/9、8/8 |

干净副本初始没有 .git、node_modules、.venv 或工具链，先执行 `bootstrap.py --app --install-node`，再 `--app --offline` 和独立 prepare。使用副本自己的 Node/npm，从副本外 cwd 通过 `npm --prefix <fresh> run ...` 连续两轮运行 typecheck、A1、A2 两项和 A3 两项，全部通过，应用 lock 不变。安装/下载阶段与 SDK 测试阶段分开。

每轮 SDK 测试重新构造临时 HOME/workspace/agentDir/缓存，不继承 Provider 或 npm 凭据；五种网络 tripwire 自检均以预期失败退出。包子进程模式额外验证 OS 读取与网络拒绝，以及 Node 写入/addon/worker 限制。npm 需要的祖先 metadata 与 `/dev/null`、系统库权限见 [具体边界](a3-boundaries.md)，没有删除安全断言或退回开放网络。清理时仅解除本轮临时快照的只读目录位并删除这些临时数据。

## 失败、限制和状态

基线三项检查无失败。复核将扩展陷阱改为发行包实际发现的 .js，并先断言全局/项目路径可被发现，再验证不加载；本报告使用修正后的代码提交和重跑结果。开发中发现 Node 不支持 TS 参数属性、Pi 祖先发现越界、npm 祖先 lstat 与 Node 子树权限不兼容；分别改用可擦除语法、显式临时发现边界和受 OS 约束的 npm 子进程读权限，最终严格检查及新副本均通过。预期故障（缺包、无效 ref、解析/保存失败、越界和网络）仍保持失败断言。

[A3 边界记录](a3-boundaries.md) 保留加载器隐式安装、发现/全局回退、原地升级、非原子快照、宿主治理和测试 profile 的限制。完整签名/撤销、安装取消/超时、活动引用回收、外部 CLI 对账、产品激活数据库和真实模型技能使用均未验证；Windows/Linux 安装子进程模式未实测。

skills、package-manager 与 SKL-01、PKG-01 进入 in_progress；resources、BOOT-03 保持 in_progress 并追加范围证据。SKL-02/PKG-02 的完整产品工作不晋级 done；CORE-02、CORE-04、SEC-02 和三个 M0 Gate 的未完成状态不变。本轮停止于 A3，下一轮才进入 A4。

原始输出在忽略的 `.artifacts/a3/`，持久引用仅指向本报告、边界和输入摘要，新克隆可定位。后续文档提交只更新证据与状态，完成前再检查 diff、秘密标记、误提交文件及新旧检查。推送成功以独立读回远端 SHA 为准，不依赖本地跟踪引用。
