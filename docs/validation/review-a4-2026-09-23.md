# A4 审核 F01–F03 复审与修正

日期：2026-09-23（Asia/Taipei）。结论：**F01、F02 在当前分支仍成立，已修正并补回归；F03 是需要明确的首版约束，已选择同 provider 单账户并加入配置检查。** 未发现需要重选技术方向或重写 Pi 的理由。本轮只处理审核，不继续实施 B Worker/IPC。

原审核针对 `cd5ba58acb03beaa3594c4dabf8afd32fc93a36f`；实际修正基线为含 B 核心的 `0b8a0317f8f5b78df63d2c44e6a306f9a83c96f8`，分支 `codex/review-a4-fixes`。被测代码先提交为 **`9b19f8ec2b2a6222c1baf58d2a991108bd7d3cba`**，再在干净工作树执行下列完整检查，最后登记本报告/SSOT。原审核文件保留，SHA-256 为 `fb6aadbe4ae0e8c0b33f4448b893854ee2e774bec59ac976ff68d0b3d63b363a`；本报告记录足够复现的结论，不要求新克隆访问仓库外的审核文件。

## 复审结论

| 审核项 | 当前复核 | 修正与证据 |
|---|---|---|
| F01 关闭/替换并发 | 被审与修正基线的 ProbeBinding Git blob 相同，`9d5409b4b2557fb969197489c8f5b532702fa50e`。真实 Pi + 合成等待门控下，原实现的 4 条新测试失败 | 记录在途替换，关闭立即使绑定不可用，等 factory/rebind 结算后由原 Runtime 释放；绑定入口/await 后/发布前均拒绝关闭状态。重复 close 返回同一清理 promise，失败保留为失败 |
| F02 跨盘范围判断 | resource-probe blob 相同，`c4354f7a2cc2e0dd3c753dc03aa8fb4536fc1db9`。原谓词仅注入 path flavor，Win32 跨盘和 UNC→盘符两个测试失败 | 使用标准库 isAbsolute(relativeResult)，不自写路径解析。3 组共 20 个 POSIX/Win32/UNC 词法用例通过；不是 Windows 实机或 reparse point 证明 |
| F03 accountId | 确认旧投影只按 providerId 查询，accountId 不形成命名空间；没有据此认定真实凭据泄漏 | M0 每个 provider 一个账户。createAuthViewReader 一次性绑定该 Runtime 的完整白名单，拒绝重复账户/重复 provider、二次绑定及未知账户；复制并冻结配置后查询，只暴露原白名单投影 |

F01 增加 5 项真实 SDK 生命周期回归：factory 等待期间关闭、实际 bindExtensions 等待期间关闭、并发关闭成功、并发关闭失败、订阅 hook 同步重入关闭。factory/bind 的等待及 cleanup 故障是明确合成注入；Session/Runtime 创建、替换、订阅和成功清理仍调用实际 Pi 公共 API。计数包装器委托真实 subscribe/dispose，验证 dispose 已调用、跟踪订阅归零、无新绑定及旧回调不发布。关闭失败用例的实际资源由测试 finally 清理，不能把人工清理称为宿主自动恢复。

F02 的复现输入包括 `C:\approved` → `D:\outside\SKILL.md`，以及 UNC 根 → `C:\approved\SKILL.md`；Win32 relative 在这些情况下给出绝对路径，旧谓词误判 true。修正同时覆盖同根、子目录、父目录、同盘其他根、大小写/分隔符、不同 UNC share/server。生产调用保持当前平台默认 path 实现，参数化只让相同函数接受标准库 path.win32/path.posix 测试。词法判断不替代符号链接/TOCTOU 后端检查。

F03 增加 2 项测试：配置别名在查询任何凭据前被拒绝；外部修改原配置不改变已绑定账户/模型标签，实际 Pi 的合成登录/注销仍改变该固定 provider 状态。logout 不解除绑定；没有账户切换或自动解绑。多个 Runtime 共享 store 仍共享凭据，本轮不验证多账户隔离。UI 请求/账户 generation、设置/IPC 入口和系统存储留在 SEC-03 的未完成验收中，不构建 IAM 或另写 OAuth。

## 复用和范围

沿用 Node **24.21.0** / npm **11.19.0** / Pi **0.87.0**，无新依赖、无运行时补丁、无 deep-import。Context7 的官方 SDK 文档用于定位生命周期，再核对已安装发行包的公开声明和实际 Runtime 行为；滚动文档不替代精确发行 API。路径语义使用 Node 标准库，在本机实际执行。

只修改 3 个探针模块、其测试及相应契约/计划/证据。ProbeBinding 仍是探针辅助类；没有把它改名为生产服务。类型补丁保持原 ADR 范围，未扩展为运行时补丁或通用补丁平台。`NEXT_STEPS.md` 顶部只保留一个当前开发项，下方历轮“下一步”改为阶段记录。

基线三项仓库检查均通过。环境为 macOS **27.0 arm64**，Node 进程 **arm64**，Rosetta **0**；Git **2.55.0** `/opt/homebrew/bin/git`，Python **3.14.7** 使用已有 `<repo>/.venv/bin/python`。Node/npm 使用项目 `.artifacts/toolchains/node-v24.21.0-darwin-arm64/bin`。唯一锁 SHA-256 仍为 `7cd35e4fc73c7e0b61112b515c6389505baf7d541e638bd7cf0ffe2dbf6e8e44`。

## 已运行的检查

以下均在上述代码提交的干净树上运行；原始日志留在忽略的 `.artifacts/review-a4/`。SDK 每次使用独立临时目录、空/合成凭据、事先批准资源、白名单环境和原有禁网/文件/进程限制，零真实 Provider 或模型调用。

| 命令 | 实际结果 |
|---|---|
| `.venv/bin/python scripts/bootstrap.py --offline` | 连续 2 次通过，默认文档行为不变 |
| `.venv/bin/python scripts/bootstrap.py --app --offline` | 连续 2 次通过；禁 lifecycle scripts，显式原声明补丁，锁不变 |
| `npm run check:environment` / `npm run typecheck` | 环境与严格完整类型检查通过 |
| `npm run test:pi-types` | 10/10 |
| `npm run test:pi-probe` | 15/15，含新增 F01 5 项及原替换三类失败 |
| `npm run test:pi-tools` / `npm run test:pi-shell` | 19/19，实际 macOS Bash 4/4 |
| `npm run test:pi-resources` / `npm run test:pi-packages` | 12/12（原 9 项 + 3 组路径测试），实际离线 npm/Git 8/8 |
| `npm run test:pi-auth` | 18/18（原 16 项 + F03 2 项） |
| `npm run test:product-core` / `npm run test:product-sdk` | B 核心 17/17、接入 5/5 |
| `.venv/bin/python scripts/check-ssot.py` | 60/60 |
| `.venv/bin/python scripts/test-tools.py` | 15/15 |
| `.venv/bin/python scripts/check-docs.py --structural-only` | 通过，startup 27 个摘要不变 |
| `.venv/bin/python scripts/check-docs.py --typecheck` | 16 个历史示例及协议类型检查通过 |

F01/F02 保留修正前失败日志，修正后的定向检查与提交后的完整回归均通过，无 skip/todo。复用已有 npm/A3 测试输入缓存，本轮没有获取新的依赖或下载真实凭据。没有额外创建新克隆、执行 Windows/Linux 实机或核验远端 Actions；不能把本机结果算作独立 CI。Mac 无模型 CI 仍是后续工程任务，不要求模型凭据才能运行。

CORE-02、SKL-01、SEC-03 继续 **in_progress**，新增/补充逐条验收与限定证据；三个 M0 Gate 继续 **pending**。B 的真实 Worker/IPC、实际崩溃/退出监督和完整产品恢复仍未实现。本轮不改变 `docs/startup/` 或原审核结论文件。

证据/导航更新后再次运行 SSOT、脚本和完整文档/协议检查，全部通过。提交前复查本轮 15 个变更文件和 diff；常见私钥、Provider/GitHub token、含凭据 URL、个人绝对路径无匹配，不将此扫描称为完整秘密审计。没有 any/ts-ignore/deep-import；依赖锁、工具链配置、startup 和原审核文件摘要不变。
